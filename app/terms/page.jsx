import TermsClient from './TermsClient';

export const metadata = {
    title: 'Terms of Use | StroyMarket',
    description: 'Rules for posting ads and using the StroyMarket service.',
};

export default function TermsPage() {
    return <TermsClient />;
}
