import SafetyClient from './SafetyClient';

export const metadata = {
    title: 'Safety | StroyMarket',
    description: 'Safety tips and disclaimer.',
};

export default function SafetyPage() {
    return <SafetyClient />;
}
